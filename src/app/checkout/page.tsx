import CheckoutPage from '@/components/pages/OrderCheckoutPage';

export default function Checkout() {
  return (
    <CheckoutPage
      // cartItems={sampleCartItems}
      // getTotalCartAmount={getTotalCartAmount}
      // formatPrice={formatPrice}  
    />
  );
}
